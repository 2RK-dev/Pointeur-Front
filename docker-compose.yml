version: "3.8"

services:
  nextjs:
    image: hhh # Si vous avez déjà une image construite
    ports:
      - "3000:3000" # Utilisation du port défini dans .env
    environment:
      - PORT=3000
      - API_URL=http://192.168.201.215:8000
